## 004 指针寻找最大值

实现 max_ptr 函数，返回数组中第一个最大元素的地址。主函数输出该元素的值和下标。

#### 输入格式：

第一行输入 n，第二行输入 n 个整数。

#### 输出格式：

输出最大值及其第一次出现的下标，下标从 0 开始。

#### 示例 1：

> 输入：6
>
> 2 9 4 9 1 3
>
> 输出：9 1

#### 示例 2：

> 输入：3
>
> -8 -2 -5
>
> 输出：-2 1

#### 提示：

- 1 ≤ n ≤ 100000。
- 下标可由返回指针与首地址相减得到。

