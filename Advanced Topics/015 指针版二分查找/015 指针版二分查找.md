## 015 指针版二分查找

在升序数组中查找目标值，返回第一个等于目标值的元素指针；不存在则返回尾后指针。

#### 输入格式：

第一行输入 n 和 target，第二行输入 n 个升序整数。

#### 输出格式：

找到时输出下标，否则输出 -1。

#### 示例 1：

> 输入：6 2
>
> 1 2 2 2 5 8
>
> 输出：1

#### 示例 2：

> 输入：4 7
>
> 1 3 6 9
>
> 输出：-1

#### 提示：

- 0 ≤ n ≤ 100000。
- 函数接口使用 begin 和 end 两个指针表示半开区间。

