## 017 只读视图校验和

定义只读字节视图，由 const unsigned char * 和长度组成。计算所有字节值之和对 256 取模。

#### 输入格式：

第一行输入 n，第二行输入 n 个 0 到 255 的整数。

#### 输出格式：

输出校验和值。

#### 示例 1：

> 输入：4
>
> 1 2 3 255
>
> 输出：5

#### 示例 2：

> 输入：3
>
> 0 128 128
>
> 输出：0

#### 提示：

- 0 ≤ n ≤ 1000000。
- 计算函数不得修改数据，接口应体现 const 正确性。

