## 026 字符串深拷贝

实现 string_clone，为源字符串申请独立空间并复制内容。修改副本的首字符后，输出原串和副本，证明两者互不影响。

#### 输入格式：

输入一行非空字符串，以及下一行的替换字符。

#### 输出格式：

第一行输出原串，第二行输出修改后的副本。

#### 示例 1：

> 输入：memory
>
> M
>
> 输出：memory
>
> Memory

#### 示例 2：

> 输入：abc
>
> z
>
> 输出：abc
>
> zbc

#### 提示：

- 字符串长度不超过 100000。
- 分配空间时不要漏掉字符串结束符。

