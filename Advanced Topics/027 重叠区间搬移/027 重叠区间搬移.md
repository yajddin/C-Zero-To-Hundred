## 027 重叠区间搬移

实现 my_memmove，在源区间与目标区间重叠时仍能正确复制。给定字符数组以及源、目标、长度，输出搬移后的数组。

#### 输入格式：

第一行输入不含空格的字符串，第二行输入 src、dst 和 len。

#### 输出格式：

输出搬移后的字符串。

#### 示例 1：

> 输入：abcdef
>
> 0 2 4
>
> 输出：ababcd

#### 示例 2：

> 输入：abcdef
>
> 2 0 4
>
> 输出：cdefef

#### 提示：

- 所有区间均保证在数组范围内。
- 根据地址关系选择从前向后或从后向前复制。

