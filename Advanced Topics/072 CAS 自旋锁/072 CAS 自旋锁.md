## 072 CAS 自旋锁

使用 atomic_flag 实现自旋锁，并用它保护共享整数数组的追加操作。t 个线程各追加自己的编号 k 次。

#### 输入格式：

输入线程数 t 和每线程追加次数 k。

#### 输出格式：

输出最终长度，以及每个线程编号在数组中的出现次数。

#### 示例 1：

> 输入：3 2
>
> 输出：6
>
> 2 2 2

#### 示例 2：

> 输入：1 3
>
> 输出：3
>
> 3

#### 提示：

- t × k ≤ 1000000。
- 加锁用 acquire，解锁用 release；临界区保持尽可能短。

