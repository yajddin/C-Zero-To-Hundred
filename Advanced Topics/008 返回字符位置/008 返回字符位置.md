## 008 返回字符位置

实现 find_char，返回目标字符第一次出现位置的指针；若不存在则返回空指针。

#### 输入格式：

第一行输入字符串，第二行输入一个目标字符。

#### 输出格式：

若找到，输出其下标；否则输出 -1。

#### 示例 1：

> 输入：banana
>
> n
>
> 输出：2

#### 示例 2：

> 输入：memory
>
> z
>
> 输出：-1

#### 提示：

- 字符串长度不超过 10000。
- 不能返回局部变量的地址。

