## 064 条件变量三人接力

创建 A、B、C 三个线程，重复 r 轮，严格输出 ABCABC。任一线程可能先启动，但输出顺序必须稳定。

#### 输入格式：

输入轮数 r。

#### 输出格式：

输出长度为 3r 的字符串。

#### 示例 1：

> 输入：3
>
> 输出：ABCABCABC

#### 示例 2：

> 输入：1
>
> 输出：ABC

#### 提示：

- 1 ≤ r ≤ 100000。
- 条件变量等待必须放在 while 循环中，以处理虚假唤醒。

