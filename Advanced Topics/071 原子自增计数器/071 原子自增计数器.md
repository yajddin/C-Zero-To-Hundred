## 071 原子自增计数器

不用互斥锁，创建 t 个线程，每个线程对 C11 原子计数器递增 k 次，输出最终值。

#### 输入格式：

输入 t 和 k。

#### 输出格式：

输出最终计数器值。

#### 示例 1：

> 输入：8 100000
>
> 输出：800000

#### 示例 2：

> 输入：2 0
>
> 输出：0

#### 提示：

- 1 ≤ t ≤ 64，0 ≤ k ≤ 10000000。
- 只关心最终计数时可使用 relaxed 内存序；说明它为何足够。

