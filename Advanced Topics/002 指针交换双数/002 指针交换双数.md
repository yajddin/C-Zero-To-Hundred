## 002 指针交换双数

编写 swap 函数，接收两个整数的地址并交换它们。输出交换后的两个整数。

#### 输入格式：

一行输入两个整数 a 和 b。

#### 输出格式：

按交换后的顺序输出两个整数。

#### 示例 1：

> 输入：3 9
>
> 输出：9 3

#### 示例 2：

> 输入：-5 -5
>
> 输出：-5 -5

#### 提示：

- swap 的返回类型应为 void。
- 不允许使用全局变量。

