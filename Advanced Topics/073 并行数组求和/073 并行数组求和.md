## 073 并行数组求和

将大数组按连续区间分给 t 个线程，每个线程计算 64 位局部和，主线程连接后归约。

#### 输入格式：

第一行输入 n 和 t，第二行输入 n 个整数。

#### 输出格式：

输出所有元素之和。

#### 示例 1：

> 输入：5 2
>
> 1 2 3 4 5
>
> 输出：15

#### 示例 2：

> 输入：4 3
>
> -10 2 3 1
>
> 输出：-4

#### 提示：

- 1 ≤ n ≤ 50000000，1 ≤ t ≤ 64。
- 划分应覆盖每个元素恰好一次，并避免多个线程频繁写同一缓存行。

