## 006 指针计算字符串长度

实现 my_strlen：从字符串首地址开始移动指针，直到遇到字符串结束符。

#### 输入格式：

输入一行不含换行符的字符串。

#### 输出格式：

输出字符串长度。

#### 示例 1：

> 输入：pointer
>
> 输出：7

#### 示例 2：

> 输入：C language
>
> 输出：10

#### 提示：

- 字符串长度不超过 10000。
- 不允许调用 strlen，也不允许使用下标访问字符。

