## 016 原地分割单词

给定一行文本，把连续空白视为分隔符。直接在原字符数组中写入结束符，并把每个单词的首地址保存到指针数组。

#### 输入格式：

输入一行可能包含多个空格或制表符的文本。

#### 输出格式：

第一行输出单词数，随后逐行输出每个单词。

#### 示例 1：

> 输入：C   pointer  lab
>
> 输出：3
>
> C
>
> pointer
>
> lab

#### 示例 2：

> 输入：   memory
>
> 输出：1
>
> memory

#### 提示：

- 文本长度不超过 4095。
- 不为每个单词单独复制内存。

