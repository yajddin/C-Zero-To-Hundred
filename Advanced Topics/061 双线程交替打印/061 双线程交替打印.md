## 061 双线程交替打印

创建两个线程：A 负责奇数，B 负责偶数。它们必须严格交替输出 1 到 n，主线程等待二者结束。

#### 输入格式：

输入一个正整数 n。

#### 输出格式：

一行输出“线程名:数字”序列。

#### 示例 1：

> 输入：6
>
> 输出：A:1 B:2 A:3 B:4 A:5 B:6

#### 示例 2：

> 输入：1
>
> 输出：A:1

#### 提示：

- 1 ≤ n ≤ 100000。
- 使用 C11 线程、互斥锁和条件变量；不要用忙等待或 sleep 控制顺序。

