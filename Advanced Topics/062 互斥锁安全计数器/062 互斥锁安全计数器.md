## 062 互斥锁安全计数器

创建 t 个线程，每个线程把共享计数器递增 k 次。使用互斥锁保证最终结果准确。

#### 输入格式：

输入线程数 t 和每线程递增次数 k。

#### 输出格式：

输出最终计数器值。

#### 示例 1：

> 输入：4 100000
>
> 输出：400000

#### 示例 2：

> 输入：1 7
>
> 输出：7

#### 提示：

- 1 ≤ t ≤ 64，1 ≤ k ≤ 1000000。
- 检查线程创建与连接结果；正确初始化和销毁互斥锁。

