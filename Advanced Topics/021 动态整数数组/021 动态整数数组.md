## 021 动态整数数组

运行时读取长度 n，动态申请恰好能容纳 n 个整数的内存，读入数据后输出平均值，最后释放内存。

#### 输入格式：

第一行输入 n，第二行输入 n 个整数。

#### 输出格式：

输出平均值，保留两位小数。

#### 示例 1：

> 输入：4
>
> 1 2 3 4
>
> 输出：2.50

#### 示例 2：

> 输入：3
>
> -3 0 6
>
> 输出：1.00

#### 提示：

- 1 ≤ n ≤ 1000000。
- 必须检查内存申请是否成功，并确保每条退出路径都释放内存。

