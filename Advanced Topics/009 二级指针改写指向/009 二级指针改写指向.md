## 009 二级指针改写指向

有两个整数 a、b 和一个起初指向 a 的指针 p。实现 choose_larger，通过二级指针让 p 改为指向较大的整数；相等时仍指向 a。

#### 输入格式：

一行输入两个整数 a 和 b。

#### 输出格式：

输出 p 最终指向的值，以及 A 或 B 表示它指向哪个变量。

#### 示例 1：

> 输入：4 9
>
> 输出：9 B

#### 示例 2：

> 输入：7 7
>
> 输出：7 A

#### 提示：

- choose_larger 的参数应包含 int **。
- 不要复制较大值，只修改指针本身。

