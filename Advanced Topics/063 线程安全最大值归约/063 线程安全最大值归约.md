## 063 线程安全最大值归约

把数组均匀分给 t 个线程。每个线程计算局部最大值，再安全地合并为全局最大值。

#### 输入格式：

第一行输入 n 和 t，第二行输入 n 个整数。

#### 输出格式：

输出数组最大值。

#### 示例 1：

> 输入：6 3
>
> 1 9 2 8 4 7
>
> 输出：9

#### 示例 2：

> 输入：3 8
>
> -5 -2 -7
>
> 输出：-2

#### 提示：

- 1 ≤ n ≤ 10000000，1 ≤ t ≤ 64。
- t 可以大于 n；避免所有元素都在全局锁内处理。

