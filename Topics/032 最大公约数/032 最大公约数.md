## 032 最大公约数

给定两个正整数，求它们的最大公约数。

#### 输入格式：

一行输入两个正整数 `a` 和 `b`。

#### 输出格式：

输出 `a` 与 `b` 的最大公约数。

#### 示例 1：

> 输入：24 18
>
> 输出：6

#### 示例 2：

> 输入：17 13
>
> 输出：1

#### 提示：

- `1 <= a, b <= 1000000000`。
- 可以使用欧几里得算法：反复令 `(a,b) = (b,a%b)`。

