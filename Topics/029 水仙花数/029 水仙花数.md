## 029 水仙花数

水仙花数是一个三位数，其各位数字的立方和等于它本身。判断给定的三位数是否为水仙花数。

#### 输入格式：

输入一个三位正整数 `n`。

#### 输出格式：

若 `n` 是水仙花数，输出 `YES`；否则输出 `NO`。

#### 示例 1：

> 输入：153
>
> 输出：YES

#### 示例 2：

> 输入：370
>
> 输出：YES

#### 示例 3：

> 输入：123
>
> 输出：NO

#### 提示：

- 153 = 1³ + 5³ + 3³。
- 分别取出百位、十位和个位。

