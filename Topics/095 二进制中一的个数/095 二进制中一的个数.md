## 095 二进制中一的个数

给定一个无符号整数，统计其二进制表示中 1 的个数。

#### 输入格式：

输入一个 32 位无符号整数 `n` 的十进制表示。

#### 输出格式：

输出二进制中 1 的数量。

#### 示例 1：

> 输入：11
>
> 输出：3

#### 示例 2：

> 输入：0
>
> 输出：0

#### 示例 3：

> 输入：4294967295
>
> 输出：32

#### 提示：

- 使用 `unsigned int`。
- 表达式 `n & (n-1)` 可以消去最低位的一个 1。

