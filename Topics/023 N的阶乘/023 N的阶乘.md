## 023 N的阶乘

给定非负整数 `n`，计算 `n!`。

#### 输入格式：

输入一个整数 `n`。

#### 输出格式：

输出 `n!`。

#### 示例 1：

> 输入：5
>
> 输出：120

#### 示例 2：

> 输入：0
>
> 输出：1

#### 提示：

- `0 <= n <= 20`。
- 使用 `unsigned long long` 保存结果。

