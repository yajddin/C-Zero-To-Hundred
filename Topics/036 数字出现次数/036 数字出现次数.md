## 036 数字出现次数

给定正整数 `n` 和一位数字 `d`，统计从 1 到 `n` 的所有整数中，数字 `d` 一共出现了多少次。

#### 输入格式：

一行输入整数 `n` 和数字 `d`。

#### 输出格式：

输出数字 `d` 的总出现次数。

#### 示例 1：

> 输入：13 1
>
> 输出：6

#### 示例 2：

> 输入：20 0
>
> 输出：2

#### 提示：

- `1 <= n <= 10000`，`0 <= d <= 9`。
- 数字 0 不能作为一个正整数的前导数字计数。

