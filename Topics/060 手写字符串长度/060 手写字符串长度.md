## 060 手写字符串长度

给定一行字符串，不使用 `strlen`，求其中字符的个数。输入末尾的换行符不计入长度。

#### 输入格式：

输入一行长度不超过 1000 的字符串，字符串可能包含空格。

#### 输出格式：

输出字符串长度。

#### 示例 1：

> 输入：Hello C
>
> 输出：7

#### 示例 2：

> 输入：abc 123!
>
> 输出：8

#### 提示：

- 使用字符数组保存字符串。
- 遍历到字符串结束标志 `\0`。

