## 010 正数负数或零

判断一个整数与 0 的大小关系。

#### 输入格式：

输入一个整数 `n`。

#### 输出格式：

若 `n > 0`，输出 `positive`；若 `n < 0`，输出 `negative`；否则输出 `zero`。

#### 示例 1：

> 输入：15
>
> 输出：positive

#### 示例 2：

> 输入：0
>
> 输出：zero

#### 示例 3：

> 输入：-8
>
> 输出：negative

#### 提示：

- 使用 `if...else if...else` 完成判断。

