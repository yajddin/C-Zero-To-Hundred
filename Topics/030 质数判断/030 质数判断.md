## 030 质数判断

给定一个整数，判断它是否为质数。质数是大于 1 且只有 1 和自身两个正因数的整数。

#### 输入格式：

输入一个整数 `n`。

#### 输出格式：

若 `n` 是质数，输出 `YES`；否则输出 `NO`。

#### 示例 1：

> 输入：17
>
> 输出：YES

#### 示例 2：

> 输入：1
>
> 输出：NO

#### 示例 3：

> 输入：25
>
> 输出：NO

#### 提示：

- `-100000 <= n <= 100000`。
- 只需尝试除数到 `sqrt(n)`。

