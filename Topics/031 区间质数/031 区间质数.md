## 031 区间质数

给定两个整数 `l` 和 `r`，按从小到大的顺序输出闭区间 `[l,r]` 内的所有质数。

#### 输入格式：

一行输入两个整数 `l` 和 `r`。

#### 输出格式：

一行输出区间内的所有质数，用空格分隔；若没有质数，输出 `none`。

#### 示例 1：

> 输入：10 20
>
> 输出：11 13 17 19

#### 示例 2：

> 输入：24 28
>
> 输出：none

#### 提示：

- `1 <= l <= r <= 10000`。
- 可把质数判断封装成函数。

