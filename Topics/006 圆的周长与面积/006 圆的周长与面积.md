## 006 圆的周长与面积

给定圆的半径 `r`，计算圆的周长和面积。圆周率取 `3.1415926`。

#### 输入格式：

输入一个实数 `r`。

#### 输出格式：

依次输出周长和面积，保留两位小数，用空格分隔。

#### 示例 1：

> 输入：1
>
> 输出：6.28 3.14

#### 示例 2：

> 输入：2.5
>
> 输出：15.71 19.63

#### 提示：

- `0 < r <= 1000`。
- 周长为 `2 * PI * r`，面积为 `PI * r * r`。

