## 063 回文字符串

判断一个非空字符串是否为回文串。比较时区分大小写，空格和标点也作为普通字符。

#### 输入格式：

输入一行长度不超过 1000 的字符串。

#### 输出格式：

是回文串输出 `YES`，否则输出 `NO`。

#### 示例 1：

> 输入：level
>
> 输出：YES

#### 示例 2：

> 输入：Level
>
> 输出：NO

#### 示例 3：

> 输入：a b a
>
> 输出：YES

#### 提示：

- 从两端向中间逐对比较字符。

