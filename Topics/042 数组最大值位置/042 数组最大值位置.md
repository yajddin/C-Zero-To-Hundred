## 042 数组最大值位置

找出数组中的最大值及它第一次出现的位置。

#### 输入格式：

第一行输入整数 `n`；第二行输入 `n` 个整数。

#### 输出格式：

输出最大值和它的下标，用空格分隔；下标从 0 开始。

#### 示例 1：

> 输入：
>
> 5
> 2 9 3 9 1
>
> 输出：9 1

#### 示例 2：

> 输入：
>
> 3
> -8 -2 -5
>
> 输出：-2 1

#### 提示：

- `1 <= n <= 1000`。
- 只有遇到更大的值时才更新位置。

