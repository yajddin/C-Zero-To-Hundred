## 051 冒泡排序

使用冒泡排序将整数数组按非递减顺序排列。

#### 输入格式：

第一行输入整数 `n`；第二行输入 `n` 个整数。

#### 输出格式：

一行输出排序后的数组，元素用空格分隔。

#### 示例 1：

> 输入：
>
> 5
> 5 1 4 2 8
>
> 输出：1 2 4 5 8

#### 示例 2：

> 输入：
>
> 4
> 3 3 -1 0
>
> 输出：-1 0 3 3

#### 提示：

- `1 <= n <= 1000`。
- 每一轮把未排序部分的最大值交换到末尾。
- 若某一轮没有发生交换，可以提前结束。

