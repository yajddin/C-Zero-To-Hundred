## 094 宏求三数最大值

定义函数式宏 `MAX2(a,b)` 和 `MAX3(a,b,c)`，使用它们求三个整数的最大值。

#### 输入格式：

一行输入三个整数 `a`、`b`、`c`。

#### 输出格式：

输出最大值。

#### 示例 1：

> 输入：4 9 2
>
> 输出：9

#### 示例 2：

> 输入：-3 -8 -1
>
> 输出：-1

#### 提示：

- 宏参数和整个宏表达式都要加括号。
- 本题只用普通变量调用宏，不传入带自增、自减等副作用的表达式。

