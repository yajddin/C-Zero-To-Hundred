## 015 简易计算器

给定两个整数和一个运算符，完成一次加、减、乘或除运算。

#### 输入格式：

一行输入 `a op b`，其中 `op` 为 `+`、`-`、`*`、`/` 之一；除法时保证 `b != 0`。

#### 输出格式：

输出整数运算结果，除法遵循 C 语言整数除法规则。

#### 示例 1：

> 输入：12 + 5
>
> 输出：17

#### 示例 2：

> 输入：7 / 2
>
> 输出：3

#### 提示：

- 读取运算符时注意跳过前导空白。
- 适合使用 `switch` 语句。

