## 045 第二大不同值

找出数组中第二大的不同数值。

#### 输入格式：

第一行输入整数 `n`；第二行输入 `n` 个整数。

#### 输出格式：

若存在第二大的不同值，输出它；否则输出 `none`。

#### 示例 1：

> 输入：
>
> 6
> 5 1 5 3 4 4
>
> 输出：4

#### 示例 2：

> 输入：
>
> 4
> 7 7 7 7
>
> 输出：none

#### 提示：

- `2 <= n <= 1000`。
- 重复的最大值不能作为第二大不同值。

