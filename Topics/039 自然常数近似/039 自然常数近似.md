## 039 自然常数近似

使用公式 `e_n = 1/0! + 1/1! + ... + 1/n!` 计算自然常数 e 的近似值。

#### 输入格式：

输入一个非负整数 `n`。

#### 输出格式：

输出 `e_n`，保留 10 位小数。

#### 示例 1：

> 输入：0
>
> 输出：1.0000000000

#### 示例 2：

> 输入：3
>
> 输出：2.6666666667

#### 提示：

- `0 <= n <= 20`。
- 可以在循环中逐步更新当前阶乘或当前项。

