## 071 凯撒密码

给定字符串和位移量 `k`，将每个英文字母向后循环移动 `k` 位。保持字母大小写，非字母字符不变。

#### 输入格式：

第一行输入字符串；第二行输入非负整数 `k`。

#### 输出格式：

输出加密后的字符串。

#### 示例 1：

> 输入：
>
> Abc-Z!
> 2
>
> 输出：Cde-B!

#### 示例 2：

> 输入：
>
> xyz
> 3
>
> 输出：abc

#### 提示：

- `0 <= k <= 100000`，先令 `k %= 26`。
- 字符越过 z 或 Z 后要回到 a 或 A。

