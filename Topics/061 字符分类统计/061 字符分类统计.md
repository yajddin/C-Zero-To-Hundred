## 061 字符分类统计

统计一行字符串中英文字母、数字、空格和其他字符的个数。

#### 输入格式：

输入一行长度不超过 1000 的字符串。

#### 输出格式：

依次输出字母、数字、空格、其他字符的数量，用空格分隔。

#### 示例 1：

> 输入：C 2026!
>
> 输出：1 4 1 1

#### 示例 2：

> 输入：Hi, C.
>
> 输出：3 0 1 2

#### 提示：

- 输入末尾换行符不参与统计。
- 可以使用 `<ctype.h>` 中的字符判断函数。

