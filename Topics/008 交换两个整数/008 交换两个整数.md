## 008 交换两个整数

给定两个整数，交换它们的值后输出。

#### 输入格式：

一行输入两个整数 `a` 和 `b`。

#### 输出格式：

交换后输出 `a` 和 `b`，用空格分隔。

#### 示例 1：

> 输入：3 9
>
> 输出：9 3

#### 示例 2：

> 输入：-1 0
>
> 输出：0 -1

#### 提示：

- 可以使用一个临时变量保存中间值。

