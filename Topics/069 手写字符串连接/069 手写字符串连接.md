## 069 手写字符串连接

不使用 `strcat`，把第二个字符串连接到第一个字符串末尾。

#### 输入格式：

输入两行字符串，每行长度不超过 1000。

#### 输出格式：

输出连接后的字符串。

#### 示例 1：

> 输入：
>
> Hello,
>  world!
>
> 输出：Hello, world!

#### 示例 2：

> 输入：
>
> abc
> def
>
> 输出：abcdef

#### 提示：

- 先找到第一个字符串的结束位置。
- 连接后别忘了补上 `\0`。

