## 064 字母大小写转换

将字符串中的大写英文字母改为小写，小写英文字母改为大写，其他字符保持不变。

#### 输入格式：

输入一行长度不超过 1000 的字符串。

#### 输出格式：

输出转换后的字符串。

#### 示例 1：

> 输入：Hello, C!
>
> 输出：hELLO, c!

#### 示例 2：

> 输入：123 AbZ
>
> 输出：123 aBz

#### 提示：

- 可以使用 `<ctype.h>`，也可以利用 ASCII 码规律。

