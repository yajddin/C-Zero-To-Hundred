## 052 选择排序

使用选择排序将整数数组按非递减顺序排列。

#### 输入格式：

第一行输入整数 `n`；第二行输入 `n` 个整数。

#### 输出格式：

一行输出排序后的数组。

#### 示例 1：

> 输入：
>
> 6
> 64 25 12 22 11 3
>
> 输出：3 11 12 22 25 64

#### 示例 2：

> 输入：
>
> 3
> -2 -5 -1
>
> 输出：-5 -2 -1

#### 提示：

- `1 <= n <= 1000`。
- 每一轮从未排序部分选择最小值放到前面。

