## 062 字符串反转

将给定字符串中的字符顺序反转。

#### 输入格式：

输入一行长度不超过 1000 的非空字符串，可能包含空格。

#### 输出格式：

输出反转后的字符串。

#### 示例 1：

> 输入：hello
>
> 输出：olleh

#### 示例 2：

> 输入：C is fun
>
> 输出：nuf si C

#### 提示：

- 先求长度，再交换首尾字符。
- 不要把字符串结束标志一起交换。

