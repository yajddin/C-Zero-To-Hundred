## 024 整数幂

给定整数 `x` 和非负整数 `n`，计算 `x` 的 `n` 次幂。

#### 输入格式：

一行输入两个整数 `x` 和 `n`。

#### 输出格式：

输出 `x^n`。

#### 示例 1：

> 输入：2 10
>
> 输出：1024

#### 示例 2：

> 输入：-3 3
>
> 输出：-27

#### 示例 3：

> 输入：5 0
>
> 输出：1

#### 提示：

- `-10 <= x <= 10`，`0 <= n <= 10`。
- 任何非零数的 0 次幂为 1；本题约定 `0^0 = 1`。

