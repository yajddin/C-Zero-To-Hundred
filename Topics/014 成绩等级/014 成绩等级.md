## 014 成绩等级

把百分制成绩转换为等级：90–100 为 A，80–89 为 B，70–79 为 C，60–69 为 D，0–59 为 E。

#### 输入格式：

输入一个整数 `score`。

#### 输出格式：

输出对应的大写等级字母。

#### 示例 1：

> 输入：95
>
> 输出：A

#### 示例 2：

> 输入：67
>
> 输出：D

#### 示例 3：

> 输入：0
>
> 输出：E

#### 提示：

- `0 <= score <= 100`。
- 可以使用 `if` 或 `switch`。

