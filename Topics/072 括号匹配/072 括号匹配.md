## 072 括号匹配

给定一个只由 `()`、`[]`、`{}` 组成的字符串，判断其中的括号是否正确匹配。

#### 输入格式：

输入一个长度不超过 1000 的括号字符串。

#### 输出格式：

匹配正确输出 `YES`，否则输出 `NO`。

#### 示例 1：

> 输入：([]{})
>
> 输出：YES

#### 示例 2：

> 输入：([)]
>
> 输出：NO

#### 示例 3：

> 输入：(()
>
> 输出：NO

#### 提示：

- 左括号入栈；右括号必须与栈顶左括号配对。
- 结束时栈必须为空。

