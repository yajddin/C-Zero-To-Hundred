## 004 两数之和

给定两个整数 `a` 和 `b`，计算它们的和。

#### 输入格式：

一行输入两个整数 `a` 和 `b`，用空格分隔。

#### 输出格式：

输出一个整数，表示 `a + b`。

#### 示例 1：

> 输入：3 5
>
> 输出：8

#### 示例 2：

> 输入：-8 2
>
> 输出：-6

#### 提示：

- `-10000 <= a, b <= 10000`。

