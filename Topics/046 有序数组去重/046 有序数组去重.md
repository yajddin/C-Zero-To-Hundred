## 046 有序数组去重

给定一个非递减整数数组，删除重复元素，使每个数只保留一次。

#### 输入格式：

第一行输入整数 `n`；第二行输入 `n` 个非递减整数。

#### 输出格式：

第一行输出去重后的元素个数；第二行输出去重后的数组。

#### 示例 1：

> 输入：
>
> 7
> 1 1 2 2 2 4 5
>
> 输出：
>
> 4
> 1 2 4 5

#### 示例 2：

> 输入：
>
> 3
> 8 8 8
>
> 输出：
>
> 1
> 8

#### 提示：

- `1 <= n <= 1000`。
- 比较当前元素与最后一个已保留元素。

