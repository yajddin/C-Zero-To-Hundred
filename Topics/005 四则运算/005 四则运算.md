## 005 四则运算

给定两个整数，依次计算它们的和、差、积与整数商。

#### 输入格式：

一行输入两个整数 `a` 和 `b`，保证 `b != 0`。

#### 输出格式：

一行输出 `a+b`、`a-b`、`a*b`、`a/b`，结果用空格分隔；除法使用 C 语言整数除法规则。

#### 示例 1：

> 输入：8 3
>
> 输出：11 5 24 2

#### 示例 2：

> 输入：-7 2
>
> 输出：-5 -9 -14 -3

#### 提示：

- `-1000 <= a, b <= 1000`。
- 负数整数除法向 0 截断。

