## 066 单词计数

统计一行英文文本中的单词数量。单词由一个或多个连续的非空白字符组成。

#### 输入格式：

输入一行长度不超过 1000 的字符串。

#### 输出格式：

输出单词数量。

#### 示例 1：

> 输入：C language is fun
>
> 输出：4

#### 示例 2：

> 输入：   one   two  
>
> 输出：2

#### 示例 3：

> 输入：     
>
> 输出：0

#### 提示：

- 从空白字符进入非空白字符时，遇到了一个新单词。
- 可以使用 `isspace` 判断空白字符。

