## 065 压缩多余空格

整理一行英文文本：删除行首和行尾空格，并把单词之间连续的多个空格压缩成一个。

#### 输入格式：

输入一行长度不超过 1000 的字符串，只包含可见字符和空格。

#### 输出格式：

输出整理后的字符串。

#### 示例 1：

> 输入："  C   is fun  "
>
> 输出："C is fun"

#### 示例 2：

> 输入："one"
>
> 输出："one"

#### 提示：

- 示例中的双引号只用于展示字符串边界，不属于输入或输出。
- 记录上一个已输出字符是否为空格。
