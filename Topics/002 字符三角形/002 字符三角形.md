## 002 字符三角形

给定一个字符 `ch`，用它输出一个高为 3 的直角三角形。

#### 输入格式：

输入一个可见字符 `ch`。

#### 输出格式：

输出 3 行，第 `i` 行输出 `i` 个字符 `ch`。

#### 示例 1：

> 输入：*
>
> 输出：
>
> *
> **
> ***

#### 示例 2：

> 输入：A
>
> 输出：
>
> A
> AA
> AAA

#### 提示：

- 可以连续使用多条输出语句。

