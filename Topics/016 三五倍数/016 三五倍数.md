## 016 三五倍数

给定一个整数，根据它能否被 3 和 5 整除输出结果。

#### 输入格式：

输入一个整数 `n`。

#### 输出格式：

同时被 3 和 5 整除输出 `FizzBuzz`；只被 3 整除输出 `Fizz`；只被 5 整除输出 `Buzz`；否则输出 `n`。

#### 示例 1：

> 输入：15
>
> 输出：FizzBuzz

#### 示例 2：

> 输入：9
>
> 输出：Fizz

#### 示例 3：

> 输入：7
>
> 输出：7

#### 提示：

- 判断顺序会影响结果。

