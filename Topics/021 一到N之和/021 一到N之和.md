## 021 一到N之和

给定正整数 `n`，计算 `1 + 2 + ... + n`。

#### 输入格式：

输入一个正整数 `n`。

#### 输出格式：

输出前 `n` 个正整数之和。

#### 示例 1：

> 输入：5
>
> 输出：15

#### 示例 2：

> 输入：100
>
> 输出：5050

#### 提示：

- `1 <= n <= 100000`。
- 结果请使用 `long long` 保存。

