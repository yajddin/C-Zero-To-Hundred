## 056 矩阵对角线之和

给定一个 `n×n` 方阵，计算主对角线与副对角线上所有元素的和；中心元素只能计算一次。

#### 输入格式：

第一行输入整数 `n`；接下来 `n` 行输入方阵。

#### 输出格式：

输出两条对角线元素的总和。

#### 示例 1：

> 输入：
>
> 3
> 1 2 3
> 4 5 6
> 7 8 9
>
> 输出：25

#### 示例 2：

> 输入：
>
> 2
> 1 2
> 3 4
>
> 输出：10

#### 提示：

- `1 <= n <= 100`。
- 当 `n` 为奇数时，两条对角线共享中心元素。

