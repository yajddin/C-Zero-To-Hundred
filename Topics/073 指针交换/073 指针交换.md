## 073 指针交换

编写函数 `void swap(int *a, int *b)`，通过指针交换两个整数，并在主函数中调用它。

#### 输入格式：

一行输入两个整数 `a` 和 `b`。

#### 输出格式：

输出交换后的两个整数。

#### 示例 1：

> 输入：12 35
>
> 输出：35 12

#### 示例 2：

> 输入：-4 0
>
> 输出：0 -4

#### 提示：

- 函数接收的是变量地址。
- 通过解引用运算符 `*` 访问地址中的值。

