## 048 数组循环右移

将长度为 `n` 的数组循环右移 `k` 位。移出右端的元素依次回到左端。

#### 输入格式：

第一行输入 `n` 和 `k`；第二行输入 `n` 个整数。

#### 输出格式：

一行输出移动后的数组。

#### 示例 1：

> 输入：
>
> 5 2
> 1 2 3 4 5
>
> 输出：4 5 1 2 3

#### 示例 2：

> 输入：
>
> 4 6
> 7 8 9 0
>
> 输出：9 0 7 8

#### 提示：

- `1 <= n <= 1000`，`0 <= k <= 100000`。
- 先令 `k %= n`。

