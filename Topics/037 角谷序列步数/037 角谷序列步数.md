## 037 角谷序列步数

从正整数 `n` 开始：若当前数为偶数就除以 2，否则变为 `3n+1`。重复操作直到变为 1，求操作次数。

#### 输入格式：

输入一个正整数 `n`。

#### 输出格式：

输出变为 1 所需的操作次数。

#### 示例 1：

> 输入：1
>
> 输出：0

#### 示例 2：

> 输入：6
>
> 输出：8

#### 提示：

- 6 的变化过程是 6→3→10→5→16→8→4→2→1。
- `1 <= n <= 100000`，中间值使用 `long long`。

