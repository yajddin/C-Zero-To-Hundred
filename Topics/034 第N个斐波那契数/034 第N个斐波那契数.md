## 034 第N个斐波那契数

斐波那契数列定义为 `F(0)=0`、`F(1)=1`，且 `F(n)=F(n-1)+F(n-2)`。求 `F(n)`。

#### 输入格式：

输入一个非负整数 `n`。

#### 输出格式：

输出 `F(n)`。

#### 示例 1：

> 输入：0
>
> 输出：0

#### 示例 2：

> 输入：10
>
> 输出：55

#### 提示：

- `0 <= n <= 90`。
- 使用 `long long`，并用迭代避免重复计算。

